import Forge from '../sections/TheForge';

export default function ForgePage() {
  return (
    // We can reuse the Forge section component directly.
    // This is the power of the component structure we set up.
    <Forge />
  );
}